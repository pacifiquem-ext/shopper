# Playwright / E2E screenshots (local only)

This directory holds browser screenshots from Playwright MCP and `e2e-audit/run-e2e.mjs`.

- **Do not commit** image files here (gitignored).
- **Never** write screenshots to the repo root or under `client/` / `server/` source trees.
- See `AGENTS.md` §8 (browser / visual QA artifacts).
